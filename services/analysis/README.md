# Analysis Service

This folder will hold reusable indicator, signal scoring, and backtest modules.

The first implementation lives in `services/api/app/signal_engine.py` so the API can be run and tested immediately. Move shared rule logic here when it grows beyond the MVP.
